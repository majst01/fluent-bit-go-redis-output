---
version: '3'

services:
  redis:
    image: redis:alpine
    ports:
     - 6379:6379

  fluent-bit-go-redis:
    depends_on: 
      - redis
    build:
      context: .
      dockerfile: Dockerfile
